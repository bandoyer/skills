---
"mattpocock-skills": patch
---

Make implementation, TDD, diagnosis, and review follow the accepted request and its existing authorization. Select routine test interfaces without an approval stage, allow initial code inspection before a reproduction, and bound diagnostic experiments.

Review directly by default, use a panel only when selected, prevent recursive delegation, and distinguish verified blockers from optional advice. Preserve the declared review allowance across repairs and handoffs.

Use focused interviews and concise specifications rather than mandatory exhaustive planning. Align the router, documentation, and harness fallback guidance. Preserve skill invocation policies and the committed-only review boundary.
