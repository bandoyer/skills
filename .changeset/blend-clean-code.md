---
"mattpocock-skills": patch
---

tdd, code-review, implement, codebase-design, retro: point at the user's `clean-code` skill for the coding standard instead of restating it. `tdd` takes seams from an approved spec without a fresh confirmation round, records the red run before the code, and cleans with the tests green rather than deferring refactoring to review. `code-review` adds the `clean-code` handback checklist and references to the Standards axis and asks the sub-agent for test-quality findings. `implement` carries the user's workflow: propose before editing, authorization once, no PR without approval.
